/**
 * Legacy hook: dulu menyinkronkan wallet Privy ke wallet-adapter.
 * Penandatanganan on-chain memakai `@privy-io/react-auth/solana` saja — tidak ada sync adapter.
 */
export function usePrivyWalletSync() {
  /* intentionally empty */
}
